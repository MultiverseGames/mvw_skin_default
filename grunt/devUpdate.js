module.exports = {
    devUpdate: { // target
        options: {
        	updateType: 'report', //just report outdated packages 
            semver: true //stay within semver when updating
        }
    }
}
